/**
 * Render the game items itemId page and its data composition.
 * Assemble the page content exposed at this location.
 * refs: none
 */
export { default } from "../../../stats/items/[itemId]/page";
